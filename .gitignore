# binaries are downloaded at runtime, never committed
*.exe
# local/secret overrides
*.local.ps1
*.secrets
.DS_Store
# provisioning artifacts belong in the private pangolin-provisioning repo, not here
pos-*.csv
pos-*.txt
